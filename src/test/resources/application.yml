spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/sandbox
    url: jdbc:h2:file:./db/sandbox;MODE=MYSQL;DB_CLOSE_DELAY=5;DB_CLOSE_ON_EXIT=true;
    username: sandbox
    password: sandbox
    driverClassName: org.h2.Driver
#    driver-class-name: com.mysql.jdbc.Driver

#flyway:
#  enabled: true
#  url: ${spring.datasource.url}
#  user: ${spring.datasource.username}
#  password: ${spring.datasource.passowrd}
#  locations: classpath:db/migrationÅ